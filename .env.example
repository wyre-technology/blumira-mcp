# Blumira API JWT Token (required)
BLUMIRA_JWT_TOKEN=your-jwt-token-here

# Transport: stdio (default) or http
# MCP_TRANSPORT=http
# MCP_HTTP_PORT=8080
# MCP_HTTP_HOST=0.0.0.0

# Auth mode: env (default) or gateway
# AUTH_MODE=env

# Logging
# LOG_LEVEL=info
